import {Text, View} from "react-native";

const Saved = () => {
   return (
    <View className="flex-1 items-center justify-center bg-white"
    > 
      <Text className="text-3xl text-primary">Save page.</Text>
    </View>
   );
}

export default Saved